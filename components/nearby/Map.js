import { View, Text } from "react-native";

        
const Map = ({midpoint, latLngOne, latLngTwo, category, radius}) => {

    return (
        <View>
             <Text>{category}</Text>
        </View>
    );
};
export default Map;